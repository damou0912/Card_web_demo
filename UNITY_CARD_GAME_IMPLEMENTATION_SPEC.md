# 卡牌对战 Demo：Unity 实现与调用规格

## 1. 文档用途

本文档用于使用 Unity 引擎实现卡牌对战 Demo。规则基线为当前已确认的 V2 规则；卡牌数据统一来自：

`outputs/card-info-table-xlsx/card_info_v2.xlsx`

当前 V2 卡牌 ID 统一为 5 位：势力序号 2 位、品质编号 1 位、势力内序号 2 位。实际技能按势力分别存放在 `shu-card-effects.js`、`wei-card-effects.js`、`wu-card-effects.js`，每张卡牌独立一个代码块，只调用通用规则操作。界面按 ID 从统一卡牌信息表读取卡牌名称、技能名称和技能效果，实际结算按 ID 从势力效果文件读取函数，两侧不读取对方的数据。

Unity 负责表现、输入和场景管理；规则核心应保持为可测试的纯 C# 运行时，不将规则散落在 MonoBehaviour 中。

## 2. 对局配置

- 支持本地 1v1 与 PVE。
- 本地 1v1 双方可选择相同或不同的卡牌势力。
- PVE 由玩家选择卡牌势力，AI 从预设势力中随机选择。
- 每位玩家拥有独立的 20 张牌库，开局洗牌。
- 先后手由系统随机决定。
- 先手初始手牌 2 张，后手初始手牌 3 张。
- 手牌上限为 5 张。
- 牌库抽完后不生成援兵；后续抽牌失败并显示提示。

## 3. 战场与守军

- 战场为 4 x 4。
- 开局不生成破坏格。
- 开局在两个不同的随机格生成两张守军。
- 守军战力在 2 和 3 中等概率随机选择。
- 守军无卡牌势力、无所属玩家、无技能。
- 守军占据战场格，但不占领该格。
- 双方均将守军视为敌方卡牌，因此可以攻击守军。
- 守军不能主动移动，但可以被技能移动、换位或重新放置。
- 守军可以被技能增减战力；战力降至 0 不会自动摧毁。
- 守军被摧毁后原格变为空格，不触发技能，也不计入任一方场上卡牌数。

## 4. 破坏格

- 破坏格最多存在 5 格。
- 当已有 5 格破坏格时，任何新增破坏格效果无效。
- 新增破坏格必须位于战场内，且目标不能已经是破坏格。
- 目标格即使有卡牌也允许生成破坏格；先完整处理该卡牌的摧毁替代、保留、被摧毁技能和连锁，再生成破坏格。
- 破坏格不可放置、不可移动进入、不可占领。

## 5. 占领与胜负

- 占领归属按卡牌所属玩家，不按卡牌势力。
- 守军和空格不属于任何玩家。
- 双方同时影响同一空格时，该格无人占领。
- 可占领格数为 `16 - 当前破坏格数量`。
- 胜利阈值为 `floor(可占领格数 / 2) + 1`。
- 每次放置、移动、交战、技能或破坏格变化后立即检查胜负。
- 任一方达到阈值立即结束对局，并中止未完成的技能链。
- 第 30 个全局回合完整执行后，比较最终占领格数；较多者获胜，相同则平局。

## 6. 回合流程

每个全局回合只由一名玩家行动：

1. 准备阶段：恢复该玩家己方卡牌的休整和本回合移动状态。
2. 抽牌阶段：抽 1 张牌；手牌满或牌库为空时提示并跳过。
3. 回合开始技能阶段：按卡牌进入战场的先后顺序逐张结算。
4. 行动阶段：第 1 回合 1 个基础行动位，其余回合 2 个基础行动位；技能提供的额外行动位可叠加。
5. 结束阶段：清除本回合临时效果，再结算回合结束技能。
6. 切换下一位玩家。

行动位耗尽后不自动结束回合。仍有操作时点击“结束回合”需要确认；本回合没有任何可执行操作时按钮高亮且无需确认。PVE 的 AI 结束回合不弹确认。

## 7. 放置、移动与交战

### 7.1 放置

- 玩家选择手牌，再选择合法空格后立即放置，不需要额外确认或“执行行动”按钮。
- 合法空格必须在战场内、不是破坏格、没有卡牌。
- 新放置卡牌默认进入休整状态。
- 放置成功后先结算该卡自身放置技能，再结算其他卡牌反应；每一步都检查胜负。
- 技能明确写明“不进入休整”或“无视休整”时，可以覆盖默认规则。

### 7.2 移动

- 普通卡牌默认上下左右移动 1 格。
- 不可进入己方卡牌、破坏格或战场外。
- 玩家点击或长按己方卡牌时显示合法移动位置。
- 点击合法目标格后立即移动或攻击。
- 长按后拖动到合法目标格，松手即执行；非法位置松手则回到原格。
- 玩家主动选择的移动每回合最多一次；技能自动移动、换位和重新放置不计入该限制。
- 新放置卡牌在休整期间不能主动移动，但可以被攻击、被技能选中，且技能明确允许时可以执行免费攻击。

### 7.3 交战

- 攻击方战力高于防守方：防守方被摧毁，攻击方进入目标格。
- 攻击方战力低于防守方：攻击方被摧毁，防守方留在原格。
- 战力相同：双方同时被摧毁，目标格为空。
- 交战时先结算攻击方自身技能，再结算防守方自身技能，再按其他卡牌进入战场的顺序结算实时效果，最后比较最终战力。
- 免费攻击使用同一套交战规则，但不消耗行动位。
- 普通移动空格显示黄色，攻击敌方卡牌位置显示红色；不显示交战结果预测。

## 8. 技能结算约定

- 技能目标不由玩家选择；多个符合条件的目标中随机选择一个，或按技能明确的最高、最低、范围条件自动确定。
- 没有合法目标时，本次技能结算但效果无效，不补偿、不改选其他目标。
- “当此卡被摧毁时”适用于所有摧毁原因。
- 先处理摧毁替代或保留效果；卡牌未离场则不触发被摧毁技能。
- 卡牌最终离场后，其自身被摧毁技能仍触发一次，使用离场前的位置和所属玩家快照。
- 重新放置视为被摧毁后重新进场：恢复基础属性、保留所属玩家、进入休整、触发放置技能，不继承本回合状态或永久战力变化。
- 保留在原格而未离场不属于重新放置，保留原有永久变化和持续状态。
- 同一摧毁事件中，先处理被摧毁卡自身替代效果，再按其他卡牌进入战场顺序处理替代效果。
- 范围技能先固定本次目标快照，再按目标进入战场顺序逐张处理；已离场目标跳过，新进入目标不加入。
- “本回合”效果在当前行动玩家结束回合时清除；结束阶段新产生的本回合效果在结束阶段完成后立即清除。
- 永久战力变化可以叠加，不受基础战力上限限制；战力最低为 0。
- 一条技能连锁最多结算 10 次；独立事件重新计数。
- 任何一步达成胜利条件时，立即结束对局并停止未完成连锁。
- 重新放置其他卡牌视为卡牌放置成功；生成破坏格不视为卡牌放置。
- 技能生成的卡牌遵守目标位置规则，并可触发放置技能。

## 9. Unity 建议架构

```text
GameRulesRuntime      纯 C#：状态、行动合法性、技能队列、胜负
CardCatalog            ScriptableObject 或 JSON 导入后的只读卡牌定义
GamePresenter          将运行时状态映射到场景对象
BoardView              4x4 格子、守军、卡牌和颜色提示
HandView               当前玩家手牌；对手只显示数量
InputController        点击、长按、拖动、悬停
AiController           PVE 行动规划
GameEventBus           放置、移动、交战、摧毁、技能、占领、胜负事件
ReplayRecorder         可选：记录随机种子和行动事件
```

规则运行时建议提供以下入口：

```csharp
GameState CreateGame(GameMode mode, DeckSelection decks, int? seed = null);
IReadOnlyList<Cell> GetPlacementTargets(GameState state, CardId card);
IReadOnlyList<MoveTarget> GetMoveTargets(GameState state, CardId card);
ResolveResult PlaceCard(GameState state, CardUid card, Cell target);
ResolveResult MoveCard(GameState state, CardUid card, Cell target);
ResolveResult EndTurn(GameState state, bool confirmed);
```

所有入口必须返回状态变化和事件列表，便于 Unity 动画、日志和自动化测试使用。

## 10. 验收标准

- 三套牌库均可加载，每套 20 张且品质字段独立存在。
- 开局无破坏格，有两张不同位置的中立守军。
- 放置和移动均为选目标后即时执行。
- 技能无目标选择弹窗，目标按规则自动或随机处理。
- 点击、长按、悬停和拖动行为符合输入约定。
- 破坏格永不超过 5 格。
- 牌库为空时不会生成援兵。
- 技能链、重新放置、摧毁替代和即时胜负检查均有单元测试。
- 规则核心可以脱离 Unity 场景运行测试。
