# 验收状态

## 当前：抽卡流程 Demo（2026-09-23）

- 配置依照此前数据表：20 测试元五连，4/3/1/1/1 奖池，权重 7000/2000/400/82/518，同名碎片 5/15/40/100/50，300/400/500 保底 8/9/10 张。正常五张后按缺卡原始权重额外补发。仅免费测试额度，无真实支付、账号、组卡或对局库存接入。
- 纯 C# 核心由 Unity 和本地浏览器服务共用。新增独立 GachaDemo 场景与 Main 顶部招募入口；Main 覆盖页打开时暂停本地演示对局计时，关闭后恢复。未启动 Unity；实际菜单、UI、平台文件保存及 Unity API 编译仍待编辑器验收。
- 固定种子 100000 次完整收集：平均 349.68 测试元，首橙均价 318.17，最高 500，37.76% 最终轮集齐。边界权重、首抽橙、重复碎片、各档硬保底、完成停止、原状态不被失败操作修改的检查通过。
- 本地服务测试通过：静态文件白名单、Origin/进程令牌、并发同 revision 仅一次成功、重启续存、错误重置拒绝、集齐后拒绝继续抽、确认重置、磁盘写入失败回滚。测试使用独立临时存档，不触碰玩家数据库。
- 浏览器实际点击五连后显示 5 张结果，3 张新卡，普通 +5 / 稀有 +15 碎片；未收集刘备卡可查看技能、0.82% 和重复 100 碎片；刷新恢复 480 测试元与 3/10 进度。390px 竖屏 DOM 尺寸检查无横向溢出，浏览器没有错误日志。截图工具未返回可见图像，因此没有宣称完成目视美术验收。
- 已通过 38 个 C# 文件语法检查、90 个资源条目检查、既有 30 组核心／工坊检查与 140 场对局、33 项 Excel 读取及卡牌导出检查。原有用户改动保留；Card Basics / Faction Icons 未改动，未上传 GitHub 或 Railway。

## 当前：英文命名与独立势力图标表（2026-09-23）

- 按最新要求拆为 `ConfigTables/Card Basics.xlsx` 和 `ConfigTables/Faction Icons.xlsx`，每份仅含一个同名工作表。采用 Faction Icons 表示势力图标，避免 State Icons 被理解为状态图标。只改文件和工作表名称，不翻译或改写卡牌内容。
- 拆分保留原工作表、筛选表、样式、条件配色、冻结、列宽、行高和输入校验的内部文件原始字节。通过表格工具重新读取两份文件，1000 条预留行内的全部单元格值与原表一致。
- 导出器同时校验两份表，仍只生成原有 2 份 Lua 和 1 份 JSON。拖入副本时按工作表名识别卡牌／图标类型，不依赖副本文件名。旧合并表已从配置目录删除，备份为 `Artifacts/ConfigTableBackups/before-english-split-20260923.xlsx`。
- 实际通过：33 项 Excel 读取测试、卡牌编译回归、两种文件任意副本名称的读取、负战力和非法图片路径拒绝且输出校验和不变、33 个 C# 文件语法检查、83 个资源条目检查。90 张卡及默认顺序、元数据与改名前完全一致。
- 两份表已生成预览，但图像工具仍未显示可见图像，未完成目视验收；内部格式原样保留。未启动 Unity，未上传远端。抽卡 Demo 因本次用户修改要求而暂停，尚未接入。

## 历史：配置表整合（2026-09-23）

- 唯一编辑源为 `ConfigTables/卡牌配置.xlsx`，只有“卡牌”和“势力角标”两页。卡牌属性、技能文字、名单和默认顺序合为一行；实际势力只填一处，由势力映射决定图片。
- 已移除旧的两份 Excel、四份 CSV、独立名单，以及三份合并前的 Lua 和对应 `.meta`。原文件备份位于 `Artifacts/ConfigTableBackups/before-consolidation-20260923-160819.zip`，备份为本机文件，不随 Git 上传。配置目录只保留新 Excel 和 README。导出仅生成 `cards.lua`、`country-badges.lua` 和现有路径的正式卡 JSON，不会重新生成旧表。
- 数据核对通过：90 张卡的全部字段、技能换行、来源元数据、卡牌顺序及 60 张默认卡的顺序，与整合前 JSON 解析结果完全一致。Excel 的 ID 为文本，战力和默认顺序为整数数字，预留 1000 条数据行格式及输入验证。
- 实际通过：28 项 Excel 读取用例、合并卡牌编译回归、一次势力修改同步角标、默认卡组顺序独立于整行排序、Windows 一键导出与 check、83 个资源条目检查、33 个 C# 文件语法检查（0 错误）、原 30 组核心／工坊检查及 140 场模拟对局。
- 对独立 XLSX 副本填入负战力，导出失败并准确提示 `卡牌!E2`；真实 Excel 和全部生成文件校验和未变。
- 两页已生成预览并检查保存的数据和类型，但图像工具未返回可见图像，目视排版验收未完成。未启动 Excel/WPS/Unity，原生重存、Unity API 编译、JSON 角标读取及实际渲染仍待目标环境验证。
- 未改动网页版本、演示卡、工坊技能执行和用户已有项目设置，未上传远端。网页导入对照稿改放 `Artifacts/Imports/`，不再增加配置目录内的表。

以下为历史记录，涉及旧表结构的内容已由上述整合取代。

## 历史：技能展示和卡牌属性配置集中管理（2026-09-23）

- 新增 `ConfigTables/卡牌内容配置.xlsx`，内含“卡牌属性配置表”（ID、名称、实际势力、品质、基础战力）、“技能展示表”（ID、技能名、完整描述）和使用说明。90 张正式卡均已迁移。ID 为文本，基础战力为整数数字，技能描述支持换行。原角标 Excel 缺失，已从上一轮保留的工作簿恢复。
- `ConfigTables/card-catalog-index.json` 保存卡牌名单、顺序、默认卡组和原来源元数据；不重复保存卡名、数值和技能文案。统一导出两份 Excel，产生 4 份 Lua、4 份 CSV 和原路径的正式卡 JSON。原界面读取路径不变。新属性／技能 Lua 暂不替换原 UI 的 JSON 读取器。
- 已通过：迁移前后整个正式卡 JSON 解析结果完全一致（包括字段、技能换行、顺序和默认卡组）；31 项 Excel 读取用例、属性／技能编译回归、势力跨表一致性；错误 XLSX 返回单元格 E2 并保持全部源文件和输出校验和不变；Windows 一键脚本及 check 命令；85 个资源条目检查；33 个 C# 文件语法检查；原 30 组核心／工坊用例和 140 场模拟对局。
- 已生成三个工作表的预览并检查保存数据及数值类型，但当前图像工具未返回可见预览，未完成目视排版验收。未启动 Excel/WPS/Unity，原生重存、Unity API 编译和菜单实际运行仍待目标环境验证。
- 技能执行步骤、工坊库、演示卡、全局游戏参数和网页版本未迁移或修改。网页同步脚本改为仅输出对照稿，避免覆盖 Excel 生成的数据；未上传远端。保留原有用户修改。

## 历史：Excel 直接导出 Lua（2026-09-23）

- 编辑源改为 `ConfigTables/卡牌表现配置.xlsx`，含 90 张卡牌表现记录、3 个势力角标及使用说明。所有配置字段是文本，预留 1000 条文本输入行，支持表格筛选与冻结卡牌表头。CSV 改为生成的文本对照，Lua 数据结构不变。
- 新增无第三方依赖的 C# XLSX 读取器，独立命令行与 Unity 导出菜单共用。Windows `Export-CardPresentation.cmd` 已实际运行成功，支持默认工作簿和传入路径。
- 已通过：22 项 Excel 读取回归；90 张卡的 Excel→CSV/Lua 一致性与势力验证；错误工作簿导出返回失败，原 Excel 和四份输出的校验和保持不变；81 个资源条目检查；31 个 C# 文件语法检查；原有 30 组核心／工坊检查和 140 场模拟对局。
- 已独立检查保存的 XLSX：两张原生筛选表、273/12 个文本数据单元格、无公式，ID 前导零保留。三张工作表已生成预览；当前工具未返回可见图像，无法完成目视排版验收。未启动 Excel/WPS 或 Unity，原生软件重存兼容性和 Unity API 编译／菜单执行仍需目标环境验收。
- 本工具仅处理既定两张表现表；不执行 Excel 公式或 Lua 脚本，不自动更改卡牌的势力或技能。现有编辑器版本、包锁和其他用户修改未改写。

## 势力绑定的国度角标（2026-09-23）

- 采用第 1 版竖签美术，魏蓝、蜀橙、吴绿，各一张 768 × 640 透明 Sprite。新增 CSV 配表和真实 `.lua` 导出；Unity 使用 TextAsset 导入器及纯 C# 数据子集读取器，不执行 Lua 代码。
- 关联改为卡牌 ID → 表内实际 `camp` → 势力唯一图片路径；禁止逐卡任意选择另一个国度的标。导出与运行时都校验实际势力一致。
- 已接入可编辑单卡预制体（现为 16 个根／子物体）及详情页；Main 的原按钮式手牌／棋盘未改动，演示卡未编造三国归属。
- 实际通过：80 个资源条目、元数据／序列化引用检查；90 张卡的 CSV→Lua 一致性与真实势力校验；纯数据回归（错势力、同势力共图、字符串 ID、缺失／失效引用、Lua 非执行限制）；原有 30 组规则／工坊检查及 140 场模拟对局；30 个 C# 文件的 C# 9 语法检查（0 错误）。
- 新增角标 Unity EditMode 用例，未运行。未启动 Unity；实际编辑器 API 编译、Lua 导入、Sprite 导入、预制体渲染待 Unity 验收。已有用户更改的编辑器版本／包锁文件未改写。


## 单张可编辑卡牌预制体（2026-09-20）

- 已撤回上一轮误做的模块化主界面；Main、规则核心及原有 UI 恢复到该轮之前的版本。GitHub 同步工具和网页测试的既有修改保留。
- 新增真实 `Assets/CardPageTemplate/Prefabs/EditableCard.prefab`，包含 15 个根／子物体；卡名、立绘、战力、品质、技能等可在 Prefab 模式直接拖动调整，无 LayoutGroup 自动排版。
- `CardPrefabView` 只按显式调用填入文字和图片，不在 Start／Update 自动刷新，不改位置、尺寸、字号、颜色或自定义子物体。附带打开 Prefab、生成保留 Prefab 关联的预览场景菜单。
- 实际通过：67 个资源条目的元数据与引用检查、预制体全部本地对象／脚本／中文字体引用检查、25 个 C# 文件的 C# 9 语法检查（0 错误），原有 30 组核心／技能检查和 140 场对局。
- 新增 4 组 Unity EditMode 预制体测试，**未运行**。没有启动 Unity 编辑器，因此实际资源导入、Prefab 编辑、换图、预览场景生成和 UI 渲染仍待 Unity 2022.3.62f3 验收。

## 卡牌页面模板（2026-09-20）

- 新增独立 `Assets/CardPageTemplate`：内容资产、中文 Inspector、预览场景、可编辑场景生成器和预留业务事件。
- 示例资产／脚本／场景的 GUID 与内部对象引用检查通过；全项目 22 个 C# 文件语法检查通过，0 错误。
- 原有 30 组规则／技能检查和 140 场对局再次通过。
- 已编写 7 组 Unity EditMode 页面测试，但未运行 Unity 编辑器，因此页面测试、编辑器 API 编译、长文滚动、图片替换及场景生成的 UI 实测仍待目标环境验收。

## 本次已实际执行

- 通过 .NET 8 编译纯 C# 规则核心（无 UnityEngine 引用）。
- 15 组检查通过：开局、非法操作、休整与移动、交战、保护、同战力同时摧毁、属性变化、被摧毁技能、任意回合认输、回合上限、提前胜利中断后续效果、固定种子复现、牌库耗尽、配置校验、随机完整对局。
- 随机对局包含 100 个种子，覆盖 4×4 与 5×5 棋盘、手牌上限、行动数非负、战力非负、场上卡牌实例不重复及最终结算。
- 90 张正式参考卡资料导入（60 默认、30 备选）；与网页源数组单向导出，演示技能单独保存。
- 核对 Unity 官方下载接口确有 `2022.3.62f3 / 96770f904ca7`，并核对测试框架的固定依赖。
- Node.js 项目完整性检查通过：配置、固定包依赖、场景 GUID、所有资源 `.meta` 唯一性、卡表字段和中文字体 SHA-256。
- Windows PowerShell 辅助脚本语法检查通过，默认只读环境检查入口运行成功；没有执行软件安装。

## 必须在目标 Unity 环境验收

### 制作工具新增验证

- 增加 15 组纯 C# 工具检查：效果顺序、六种触发、条件、临时属性、技能摧毁与护盾、离场快照、自毁、队列上限、即时胜利、目标选择、卡组引用、数据校验、实验快照、双方独立卡组、40 场组合技能对局。
- 连同原有测试共 30 组、140 场对局通过；附带 JSON 制作库、双方卡组及两个示例场景均通过实际核心实例化验证。
- 全部 15 个 Unity C# 文件通过 C# 9 语法检查，0 个语法错误；这不包含 Unity API 语义编译。
- 目标 Unity 环境还需验收：工坊新建／复制卡牌、模板调整、保存／撤销／导入导出、测试卡组切换、实验室摆场及日志搜索、冲突检测、重新导入后数据保留。

本次没有安装／启动 Unity 编辑器，因此不能把静态检查说成完成了编辑器编译、场景运行或平台构建，更不能保证任意电脑硬件、网络、账号条件下无需排障。项目已提供以下可重复验收入口：

1. Unity Hub 导入后，Console 无红色编译错误。
2. `Card Demo > Validate Project` 成功。
3. `Window > General > Test Runner > EditMode` 全部通过。
4. `Assets/Scenes/Main.unity` Play 后，中文字体、棋盘、手牌、详情可见。
5. 己方出牌、移动、休整、AI 行动、回合计时工作；玩家信息与手牌不交换位置。
6. “展开卡牌流程”可滚动，显示属性来源、保护及摧毁原因。
7. 对方回合认输，结果显示正确获胜者 ID、比分和回合数。
8. 配置窗口修改 4×4/5×5、随机种子、时间并保存，重新 Play 后生效。
9. 16:9、16:10 和窄横屏实测布局；根据设备进一步调整字体和触控区域。
10. Windows 构建成功并独立运行；如需 WebGL，安装模块后构建并通过 HTTP 访问验证。

微信转换 SDK、微信登录、小游戏发布包、手机真机、线上联网均未验收，也未宣称已经完成。
