# 卡牌图片放置区

将自己的 PNG / JPG 卡牌立绘放在此目录。

在 Unity 中选中图片，将 Inspector 的 **Texture Type** 设置为 **Sprite (2D and UI)**，点击 **Apply**，然后拖到页面内容资产的“卡牌立绘 Sprite”栏。

没有图片也可以运行：模板自动显示立绘占位提示。立绘保持宽高比，不会拉伸。
这里只预留图片位置，不包含正式武将素材。请使用有权使用的图片。
