# 新手教程

从主菜单「新手教程」进入，无需登录。教程共四课：放置与移动、战力与交战、技能与配合、占领与胜利。每课使用明确说明的固定局面，跟随高亮手牌和格子操作即可推进；操作不会消耗或修改玩家保存的卡组。

基础两课与占领课使用无技能援军和中立守军。援军为训练专用的 1、2、3 战力卡牌，放置后归玩家所有；守军不属于任何玩家，无技能、不主动移动，双方均视其为敌军。援军不加入正式卡池，不改变挑战模式「援军」词条生成的援兵。

技能课再引入王平、廖化和魏延，演示起势和入阵，并让无技能援军接受友军强化。展示、放置、休整、抽牌、交战与胜利判定均使用正式 V2 核心；最终用援军在 4×4 棋盘上从 7 格扩张到 9 格，触发真实胜利。

训练局没有倒计时，陪练只结束回合。错误操作会显示当前目标，不改变局面。可以重练本课或退出；最后一课完成后在当前浏览器保存完成标记，并可直接进入 PVE 卡组选择。刷新会回到主菜单，不保存进行到一半的局面。

## 维护

- `tutorial.js`：课文、预设局面、步骤推进和操作限制。
- `script.js`：训练援军的独立展示数据，以卡牌 ID 统一解析手牌、棋盘和动画中的名称与战力。
- `tutorial.css`：入口、引导面板和响应式样式，沿用游戏配色。
- `core-v2.js`：独立 `tutorial` 模式及教程接入点；正常模式继续使用原规则与倒计时。
- `tutorial.test.js`：真实核心流程测试，覆盖误操作、重复点击、回合结算锁、技能结果、获胜、重练、退出及正常模式隔离。

修改教程卡牌或技能后运行 `node tutorial.test.js` 和 `node core-v2.regression.test.js`，再通过主菜单在浏览器走完四课。
