# 卡牌对战 Demo：Web Game 实现与调用规格

## 1. 文档用途

本文档用于使用 HTML、CSS 和 JavaScript 实现浏览器版卡牌对战 Demo。规则与 Unity 规格完全一致；Web 层负责 DOM、动画、输入和可视反馈，规则状态与技能结算应集中在独立的运行时模块。

卡牌信息表：

- `outputs/card-info-table-xlsx/card_info_v2.xlsx`

当前 V2 卡牌 ID 统一为 5 位：势力序号 2 位、品质编号 1 位、势力内序号 2 位。魏国阶段性独立效果实现位于 `wei-card-effects.js`；蜀、吴在迁移完成前由核心兼容分支处理。

三套 V2 数据已汇总至 `outputs/card-info-table-xlsx/card_info_v2.xlsx`；运行 `npm run generate:card-info` 可从该表格重新生成 `card-info.js`。其中表格“基础战力”导出为 `baseAttack`，`v2-card-data.js` 再映射为核心使用的 `attack`；运行时只读取其适配结果，并校验卡牌 ID 唯一、品质合法、战力为非负整数。

## 2. 页面与模块

```text
index.html              页面骨架和无障碍结构
styles.css              棋盘、卡牌、提示颜色、响应式布局
card-info.js             统一卡牌信息表的运行时导出
game-state.js            对局状态和不可变更新入口
rules-engine.js          放置、移动、交战、摧毁、技能、胜负
skill-engine.js          技能触发队列和 10 次上限
ui-renderer.js           DOM 渲染和卡牌详情
input-controller.js      点击、长按、悬停、拖动
ai-controller.js         PVE AI
animation-controller.js  移动、交战、摧毁、技能动画
```

正式入口应使用当前根目录 `index.html`；旧试验入口已删除，不进入正式规则来源。

## 3. 对局配置

- 支持本地 1v1 与 PVE。
- 本地 1v1 双方可以选择相同或不同的卡牌势力。
- PVE 玩家选择势力，AI 随机选择势力。
- 每位玩家独立使用自己的 20 张牌库并洗牌。
- 系统随机决定先手。
- 先手初始 2 张手牌，后手初始 3 张手牌。
- 手牌上限 5 张。
- 牌库抽完后不生成援兵，后续抽牌失败并显示 Toast 提示。
- 本地 1v1 和 PVE 均只显示当前玩家手牌内容；对手只显示手牌数量。

## 4. 战场、守军与破坏格

- 战场为 4 x 4。
- 开局不生成破坏格。
- 在两个不同随机格生成守军；守军战力 2 或 3 等概率随机。
- 守军无卡牌势力、无所属玩家、无技能，技能栏显示“无”。
- 双方都将守军视为敌方卡牌，可以攻击、减益或触发敌方目标效果。
- 守军不能主动移动，但允许被技能移动、换位或重新放置。
- 守军被摧毁后原格为空，不占领、不计入任一方卡牌数量、不触发技能。
- 守军战力可以被技能增减，最低为 0，降至 0 不自动摧毁。
- 破坏格最多 5 格。
- 新破坏格必须在棋盘内且不能已是破坏格；目标有卡牌时先完成该卡完整摧毁流程，再生成破坏格。
- 破坏格不可放置、不可移动进入、不可占领。

## 5. 回合和行动状态机

```text
PREPARE
  -> DRAW
  -> START_SKILLS
  -> ACTION
  -> END_CLEAR_TEMP
  -> END_SKILLS
  -> SWITCH_PLAYER
```

每回合顺序固定为：恢复己方状态 → 抽牌 → 回合开始技能 → 行动阶段。第 1 个全局回合有 1 个基础行动位，第 2～30 个全局回合有 2 个基础行动位。技能提供的额外行动位可以叠加，且可用于任意放置或移动。

行动位耗尽不自动换回合。玩家点击“结束回合”时：

- 若仍有任意可执行的放置、移动、免费移动、免费攻击或技能行动，显示确认提示。
- 若完全没有可执行行动，按钮高亮且无需确认。
- PVE 的 AI 结束回合不显示确认提示。

第 30 个全局回合仍完整执行，玩家手动结束后才进行最终占领比较。

## 6. 即时操作交互

### 6.1 放置

1. 点击手牌卡牌。
2. 棋盘显示合法空格。
3. 点击合法空格后立即放置并结算。
4. 不显示“执行行动”按钮，不要求二次确认。

新放置卡牌默认进入休整；技能明确写明“不进入休整”或“无视休整”时例外。放置结算顺序为：新卡自身放置技能 → 其他卡牌反应 → 每一步即时检查胜负。

### 6.2 移动与攻击

- 点击己方卡牌：显示可移动位置。
- 再点击合法目标格：立即移动或攻击。
- 长按卡牌：显示技能影响范围、技能效果和可移动位置，并进入拖动状态。
- 电脑端鼠标悬停：显示技能影响范围和技能效果。
- 长按拖动到合法目标格，松手立即执行；非法位置松手则恢复原位。
- 普通移动位置使用黄色底层颜色。
- 可攻击敌方卡牌的位置使用红色底层颜色。
- 技能影响范围使用蓝色底层颜色。
- 不显示交战胜负预测或“可攻破/必败/同归于尽”提示。

技能目标永远由规则引擎自动或随机确定，不弹出目标选择框。

## 7. 交战规则

- 攻击方高战力：摧毁防守方并进入目标格。
- 攻击方低战力：攻击方被摧毁，防守方留在原格。
- 战力相同：双方同时摧毁。
- 交战先结算攻击方自身技能，再结算防守方自身技能，再按其他相关卡牌进入战场顺序结算实时技能，最后比较最终战力。
- 免费攻击不消耗行动位，但遵守休整和每回合主动移动限制中技能明确保留的部分。
- 交战后的摧毁效果在交战结果确定后结算。

## 8. 技能引擎约定

### 8.1 目标与触发

- 任何技能触发后不要求用户选择目标。
- 多个合格目标中随机选择一个，或根据文本的最高、最低、范围条件自动筛选后随机选择。
- 没有合法目标时技能结算但效果无效，不补偿、不改选。
- “当此卡被摧毁时”不区分摧毁原因。
- 摧毁替代/保留先于被摧毁技能。
- 卡牌最终离场后仍使用离场前快照触发自身被摧毁技能。

### 8.2 重新放置和生成

- 重新放置等同于放置一张相同的新卡。
- 重新放置恢复基础属性、保留所属玩家、进入休整、触发放置技能，不继承永久战力、本回合效果或移动次数。
- 只保留在原格而未离场的卡牌，不算重新放置，原永久状态保留。
- 技能重新放置其他卡牌视为一次卡牌放置成功；生成破坏格不算卡牌放置。
- 被延迟效果引用的来源卡牌离场时，尚未触发的延迟效果失效。

### 8.3 顺序、快照和上限

- 回合开始技能按入场顺序逐张结算，每步重新读取当前场面。
- 同一摧毁事件的替代效果先处理被摧毁卡自身，再按入场顺序处理其他卡。
- 范围技能开始时固定目标快照；已离场目标跳过，新进入目标不加入。
- 任何一步改变占领、破坏格或胜利阈值后立即检查胜负；达成胜利时停止未完成技能链。
- 本回合效果在当前行动玩家结束回合时清除；结束阶段新产生的本回合效果在结束阶段结束后立即清除。
- 永久战力可以叠加，战力最低为 0。
- 每条连续技能链最多处理 10 次；下一条独立事件重新计数。

## 9. 推荐状态结构

```js
{
  ruleset: "v2",
  mode: "pvp" | "pve",
  turn: 1,
  phase: "prepare" | "draw" | "startSkills" | "action" | "endSkills" | "result",
  boardSize: 4,
  brokenCells: [],
  guards: [{ uid, row, col, attack }],
  boardCards: [],
  players: [{ id, deckKey, drawPile, hand, boardCardUids, isAI }],
  activePlayerId: 1,
  actionsUsed: 0,
  actionLimit: 1,
  selection: null,
  winner: null,
  skillChain: { active: false, steps: 0, queue: [] }
}
```

运行时接口建议：

```js
createGame(mode, selectedDecks, seed)
getPlacementTargets(game, cardUid)
getMoveTargets(game, cardUid)
placeCard(game, cardUid, targetCell)
moveCard(game, cardUid, targetCell)
endTurn(game, confirmed)
```

每个行动接口都应立即更新状态并返回事件列表，供动画、日志、Toast 和测试使用。

## 10. 验收标准

- 正式入口为根目录 `index.html`，只加载 V2 卡牌数据和 V2 规则核心；旧版数据不再进入运行链路。
- 三套势力牌库均为 20 张，并包含独立的卡牌品质字段。
- 开局无破坏格，有两张不同位置的中立守军。
- 破坏格永远不超过 5 格。
- 牌库为空时不生成援兵。
- 放置和移动均在目标选择后即时执行。
- 技能不出现目标选择弹窗。
- 点击、长按、悬停、拖动和三种提示颜色符合规格。
- PVP/PVE 隐藏对手手牌内容。
- 技能链、摧毁替代、重新放置、守军、动态胜利阈值均有自动化测试。
