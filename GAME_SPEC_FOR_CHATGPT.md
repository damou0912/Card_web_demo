# 卡牌对战 Demo：当前 V2 开发说明

## 唯一数据与规则来源

正式规则为 `CORE_RULES_V2.md`，卡牌信息与卡牌效果文本来自 `outputs/card-info-table-xlsx/card_info_v2.xlsx`；`card-info.js` 是该表格的运行时导出。旧版卡牌数据与旧版同步行动规则不再使用。

## 文件职责

- `index.html`：正式对局入口。
- `card-info.js`：统一卡牌信息表的运行时导出；其中 `baseAttack` 直接对应表格“基础战力”。
- `v2-card-data.js`：将统一卡牌信息适配为核心使用的卡牌槽位，不保存卡牌文字数据；核心 `attack` 从 `baseAttack` 映射而来。
- `script.js`：共享 DOM、动画和兼容辅助函数；正式玩法由 `core-v2.js` 接管。
- `core-v2.js`：V2 状态、回合、移动、交战、技能、AI 和渲染；精英 AI 的随机特性在进入挑战对局时抽取。
- `elite-ai-effects.js` / `elite-ai-info.js`：精英 AI 特性的运行逻辑与显示文案分离维护。
- `online-client.js`、`railway-server.js`：联网客户端与房间服务。
- `card-test.html`、`card-test.js`：V2 场景测试台。

## 技能显示约定

技能名称用于卡面识别；技能栏、详情面板、手牌悬停、战场悬停和测试台统一直接展示表格中的完整 `effect` 原文，原始换行照常显示，不拆分、不去编号、不优化文本。卡牌不单独显示触发词条，也不提取、派生或校验词条。概要字段不存在且不得重新引入。

## 对局约定

支持本地 1v1、标准 PVE、PVE 挑战、联网对战和 3x3/4x4/5x5 地图。PVE 挑战由精英 AI 执行：AI 开局多 1 张手牌，每回合额外获得 1 次行动，并在进入对局时随机抽取 1 个隐藏于主菜单、在对局内公布的精英特性。回合按准备、抽牌、回合开始技能、行动、结束清理、回合结束技能执行。每个行动阶段限时 5 分钟（300 秒），手牌上方显示倒计时，归零后自动结束回合；联网时由房主统一结算超时。开局生成两张中立守军，破坏格最多 5 个，牌库耗尽不生成援兵。左侧玩家信息固定为上方对方、下方我方，不随回合归属交换；联网时以本机玩家 ID 确定我方，行动数与计时器在对方回合切换为对方色。

联网大厅实时列出所有已创建房间及地图、玩家席、观战席和对局状态。每个房间固定有 2 个玩家席和 2 个观战席；玩家席满员后允许进入观战。观战者仅接收隐藏双方手牌后的对局状态，棋盘、手牌、结束回合、认输和重新开始均不可操作；玩家 1 固定显示在下方、玩家 2 固定显示在上方。观战者可随时退出，退出只释放自己的观战席，不改变玩家连接与对局状态。联网玩家可在自己或对方回合主动认输。玩家意外断线后以随机会话 Token 保留席位 5 分钟，客户端自动重连；Token 恢复成功后轮换，IP 不参与身份认证。

## 修改与验收

任何技能调整都必须同步更新 `effect` 文本、规则逻辑和边界测试；名称、战力、技能名称必须单行自适应显示。修改 `outputs/card-info-table-xlsx/card_info_v2.xlsx` 后运行 `npm run generate:card-info` 更新 `card-info.js`。测试实现位于独立的 `core-v2.test-suite.js`，不随正式页面加载；部署前运行 `core-v2.regression.test.js`、`online-client.reconnect.test.js` 与 `railway-server.integration.test.js` 并确认全部测试通过。
