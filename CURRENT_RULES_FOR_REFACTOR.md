# 当前 V2 规则与实现记录

本文档已从旧版重构基线更新为当前实现记录，完整规则见 `CORE_RULES_V2.md`。

## 数据与入口

- 正式入口：`index.html`。
- 卡牌信息表：`outputs/card-info-table-xlsx/card_info_v2.xlsx`，蜀、魏、吴共 60 张。
- 运行时卡牌适配：`card-info.js` -> `v2-card-data.js`，后者不再内嵌卡牌文字描述；表格“基础战力”导出为 `baseAttack` 并映射为核心 `attack`。
- 卡牌 ID 使用 5 位结构：势力 2 位 + 品质 1 位 + 势力内序号 2 位；例如曹洪为 `02101`。
- 旧版卡牌数据与试验入口已删除，正式入口只进入 V2 规则核心。
- 技能概要字段已删除，技能效果直接读取 `effect`。

## 分阶段技能重构

- 实际技能按势力分别位于 `shu-card-effects.js`、`wei-card-effects.js`、`wu-card-effects.js`；每个文件沿用 `wei-card-effects.js` 的结构，每张卡牌拥有独立代码块，通过核心提供的通用操作上下文执行。
- 三个势力文件只按卡牌 ID 保存实际技能，不读取卡牌展示表；删除或修改其他卡牌的展示配置不会改变已加载技能。
- `card-info.js` 只负责卡牌名称、技能名称、技能效果、势力、品质与基础战力；不生成词条字段，页面始终按卡牌 ID 查询该表格导出，不读取对局卡牌对象上的同名字段。
- 对局卡牌对象只保留 ID、战力、归属、位置和结算状态，不携带名称、技能名称、技能描述、势力、品质或词条；旧联网状态中的这些字段会被清除，旧运行时版本会被拒绝。
- 主游戏加载三个势力技能文件，Card Test 只加载表格展示数据，不加载实际技能代码。展示模块不读取技能注册表，势力技能文件也不读取任何展示字段。
- 核心统一派发放置、回合开始、摧毁、其他卡牌摧毁、移动和交战结束事件，不再由势力专属分支承担正式结算。
- 独立效果只依赖 ID、战力、归属、位置、手牌/牌库数量和通用操作（抽牌、弃牌、战力调整、摧毁、重新放置、移动和交战），不会调用展示数据或其他具体卡牌的效果代码。
- 修改统一表格后运行 `npm run generate:card-info`，即可刷新页面和对局使用的卡牌基础信息。

## 当前实现

- 支持本地 1v1、PVE、联网房间和 3x3/4x4/5x5 地图。
- 回合顺序为准备、抽牌、回合开始技能、行动、结束清理、回合结束技能。
- 开局生成两张中立守军，破坏格上限为 5。
- 放置和移动选定合法目标后立即执行；目标自动或随机确定。
- 技能链、摧毁替代、重新放置、动态占领和胜负检查均按 V2 文本执行。
- 联网会话使用服务端令牌恢复，操作请求由服务端校验并按顺序转发。

## 验证

`core-v2.regression.test.js` 加载独立的 `core-v2.test-suite.js`，覆盖规则回归、卡牌边界和卡牌覆盖测试；测试代码不进入正式页面，修改后必须全部通过。
